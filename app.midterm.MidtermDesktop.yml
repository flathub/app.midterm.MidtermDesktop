app-id: app.midterm.MidtermDesktop
runtime: org.freedesktop.Sdk
runtime-version: "20.08"
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: "20.08"
rename-desktop-file: midterm.desktop
rename-icon: midterm
separate-locales: false
command: midterm
finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=pulseaudio
  - --share=network
  - --filesystem=home
modules:
  - name: midterm
    buildsystem: simple
    build-commands:
      - ar x midterm.deb
      - tar -xf data.tar.xz
      - cp -r usr/* /app

      - mkdir /app/main
      - cp -r opt/Midterm/* /app/main

      - install -Dm644 app.midterm.MidtermDesktop.metainfo.xml /app/share/metainfo/$FLATPAK_ID.metainfo.xml
      - desktop-file-edit --set-key=Exec --set-value='midterm %U' /app/share/applications/midterm.desktop

      # Install app wrapper
      - install -D midterm /app/bin/
    sources:
      - type: file
        url: https://midterm-desktop-builds.s3.us-east-2.amazonaws.com/Midterm-1.2.0.deb
        sha512: 2e1f80783c26635b217dbc54f7b59b0658c153a1e5ecf5d34c7ba4515a1987f993cc124b180639ce7b233f9c329939084429768e06509c0eab3a74acd660954d
        dest-filename: midterm.deb
      - type: file
        path: app.midterm.MidtermDesktop.metainfo.xml
      # Wrapper to launch the app
      - type: script
        dest-filename: midterm
        commands:
          - exec zypak-wrapper /app/main/midterm "$@"
